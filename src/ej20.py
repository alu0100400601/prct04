#!/usr/bin/python
#!encoding: UTF-8

x=-b/a
a= float(raw_input('valor de a: '))
b= float(raw_input('valor de b: '))
print 'solucion: ', x
#el error es que utilizamos la operacion auntes de declarar a y b, tendria que ir la operacion justo antes del print